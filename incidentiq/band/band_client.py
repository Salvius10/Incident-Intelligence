"""
Band Client — thin wrapper around the Band multi-agent coordination API.
Provides publish/subscribe helpers so agents can post state updates and
read state from other agents without direct coupling. (Band integration added later.)
"""
